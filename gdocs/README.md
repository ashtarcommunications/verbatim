# To test

`npx clasp push` from the /gdocs folder to upload to apps script editor
Then Deploy from the editor

Important: You can't be logged in to multiple google accounts in the same browser, or the add-on will have permissions errors. Known bug for Google Apps Script.
